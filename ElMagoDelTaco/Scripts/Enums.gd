# Enums for Story Trigger - Need to restart editor or play and close the game
# to see changes!
# ONLY ADD ENUMS AT THE END OTHERWISE EVERYTHING BREAKS AND
# ALL QUESTS NEED TO BE EDITTED AGAIN!!!!!!
enum StoryTriggerCondition {
	default,
	pickup_coin,
	kill_chilli,
	talk_to_B_urrito,
	talk_to_Patua_Pechos,
	kill_el_muerto,
}

# The Upgrade Types that the player can do
enum UpgradeType {
	HEALTH,
	CAST_TIME,
	DAMAGE
}
