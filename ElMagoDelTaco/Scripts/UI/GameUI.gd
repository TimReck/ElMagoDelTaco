extends Control

export(Array, NodePath) var setVisibleNodes


####### Nathanael - Macht probleme so ########
# This is just a script so the UI is not in the secne visible the whole time
# to set it visible after startup
#func _ready():
#	for element in setVisibleNodes:
#		var node = get_node(element)
#		node.visible = true
